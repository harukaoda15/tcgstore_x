# tcgstore-x 仕様書

最終更新: 2026-03-17

## 更新ルール

- 本プロジェクトの仕様調整を行ったら、同じ変更をこのファイルにも反映する。
- 投稿文ルール、判定条件、Cron、手動テスト方法を変更した場合は必ず更新する。

## システム概要

- 実行基盤: Cloudflare Workers
- スケジュール: Cloudflare Cron Triggers
- 永続化: Cloudflare KV（`STATE`）
- 投稿先: X (Twitter)
- AI生成: Anthropic Claude API

## 現在の運用戦略（要点）

- 目的は「広告感の強い定型投稿」ではなく、読み手の判断材料になる投稿を継続すること。
- 自社商品紹介daily（`TCG STORE` / `メルカリくじ`）は一時停止し、コードはフラグで残す。
- `price_spike` で動いたカードを KV `watchlist` に自動追加し、7日無更新で自動除外する。
- 毎日21:00 JSTに `watchlist` から市場まとめ投稿を1本出す（URLなし / #ポケカ のみ）。
- 文面は「数字を具体的に」「上昇/下落を明示」「煽り/断定/予測なし」を基本方針とする。

## 実行スケジュール

- `*/30 * * * *`
  - しきい値監視（メルカリくじ: 10%/5%、TCG STORE: 5%/1%）
- `0 3 * * *`（JST 12:00）
  - 追加投稿なし（自社商品dailyは停止中）
- `0 11 * * *`（JST 20:00）
  - watchlist価格更新（`sourceUrl` 再取得で currentPrice / imageUrl を更新）
- `0 12 * * *`（JST 21:00）
  - 市場まとめ投稿（watchlistベース）

## 機能1: しきい値監視（ソース別）

- 判定対象: 取得データの残数/総数から算出した割合
- 対象ソース: `メルカリくじ` と `TCG STORE` の両方
- メルカリくじ:
  - 10%以下: `🎯` テンプレート
  - 5%以下: `🚨` テンプレート
- TCG STORE:
  - 5%以下: `🎯` テンプレート
  - 1%以下: `🚨` テンプレート
- しきい値投稿済みフラグを KV で管理し、重複投稿を防止
- ラスト賞画像がない商品は、ラスト賞訴求行を自動で省略
- メルカリくじの文体は「孤高のラッパー」トーン（静か・冷たい・少し挑発的）
  - 短文中心、過剰説明しない
  - `😮‍💨 😏 👀 🫥 👁️ 😶‍🌫️ 🫠 🫰` を少数使用
  - 安直な煽り語（例: 焦れ/急げ/爆アツ）は使わない
- 直近 `price_spike` 情報（72時間以内）を参照し、タイトルとカード名が一致する場合のみ価格スパイク行を追記
  - 追記形式: `📈 {カード名}: {前回価格}円 → {現在価格}円（+{変化率}%）`
  - 価格変化が弱い場合（`change_pct < 8`）は購買意欲を下げるため追記しない

### 手動確認

- プレビュー: `/?forceLevel=under_5` または `/?forceLevel=under_1`
- 実投稿: `/?commit=1&forceLevel=under_5` など

## 機能2: 市場まとめdaily（21:00 JST）

- データ元: KV `watchlist`（price_spikeで自動更新）
- watchlist運用:
  - 価格スパイク検出時にカードを自動追加
  - 既存カードは `lastSeenAt` を更新（初回は `firstSeenAt` を保持）
  - 最後の検出から7日経過したカードは自動除外
  - 上限20件（新しい順）
  - `source_url` から `og:image` を抽出し `imageUrl` として保存
- 投稿スタイル:
  - 1行目: `【M/D(曜)テーマ名】`
  - 曜日テーマを切替:
    - 月: `価格上昇ランキング`
    - 火: `価格下落ランキング`
    - 水: `今週の急騰ランキング`
    - 木: `高額カードランキング`
    - 金: `価格上昇ランキング`
    - 土: `今週の急騰ランキング`
    - 日: `価格上昇ランキング`
  - 本文: 3〜5件、カード間に空行を入れて読みやすくする
  - ランク表示は `🥇🥈🥉`（4位以降 `🏅`）
  - 絵文字は上昇率ルール（`🔥 / 📈 / ⚡ / ✅ / 📉`）をAI入力データとして渡す
  - 価格行は `前回→現在（3/10→3/17 +49.0%）` 形式
  - 上昇/下落方向を明示
  - 1項目は1〜2行
  - URLなし
  - ハッシュタグは `#ポケカ` 1つのみ
  - 煽り・断定・予測は禁止
  - 画像は watchlist の `imageUrl` から最大3枚添付

## 機能3: TCG STORE daily 紹介（AI）【停止中】

### 候補抽出条件

- 必須:
  - `id`, `name`, `price`, `url`, `mainImageUrl`
  - `visibility=true`
  - `status=1`
  - `stock > 0`
- 除外:
  - 売切れ、停止、主要項目欠損

### 選定ロジック

- 基本: `JST日付seed + JST時間帯seed` を使って候補を選定
- 直近投稿URLと同じ場合は回避
- 直近投稿履歴（最大10件）に含まれるURLも可能な限り回避
- 直近の `price_spike` 情報（72時間以内）がある場合、該当カード名に関連する商品を優先
- `pick_offset` 指定時は候補オフセットを追加
- `exclude_unit` 指定時は該当通貨を候補除外

### 通貨判定

- `currency` と商品名から判定:
  - `pOAS`
  - `OAS`
  - `コイン`

### 画像ルール

- daily 投稿はメイン画像1枚のみ添付
- 画像未取得の商品は候補から除外
- メルカリくじは汎用OG画像（`openGraph.png` 等）を避け、商品キービジュアルを優先取得する
- メルカリdaily画像には alt テキストを設定（`商品名 | 1等候補: ... | 残り回数`）

## 機能4: メルカリくじ daily 紹介【停止中】

- 候補: メルカリくじ一覧から取得した販売中URL
- 選定: JST日付seed + JST時間帯seed + 直近履歴回避（TCG STOREと同様）
- 直近の `price_spike` 情報（72時間以内）がある場合、関連キーワードを含むくじを優先
- 投稿形式: **AI生成（固定テンプレ廃止）**
  - 2〜4行、最終行は `{商品URL}` のみ
  - 1行目は商品名始まり禁止（読み手メリット/事実始まり）
  - ハッシュタグはS賞/1等カード由来のみ最大2個
  - 絵文字は0〜1個まで
  - 在庫表現は「まだ引ける / まだ回せる」を優先し、「残っている」は避ける
  - 「全◯回中、現在◯回」「消化率◯%」の比較表現は使わない
  - 「じっくり選んで引ける」「内容を確認してみてください」などの不要文は禁止
  - S賞/1等カード名を本文に最低1つ含める
- 画像: メイン画像1枚
- トーン:
  - しきい値追い込み投稿とは分離
  - 通常紹介は事実ベースで簡潔、露骨な煽りはしない

## AI生成仕様

### 使用データ

- TCG STORE API:
  - 商品名、価格、在庫、総口数、残り率、1日上限
  - 上位賞/次点賞のカード名
  - coin表記賞品の最小〜最大値（抽出できる場合）
  - **各ランク当選確率** (`card.supply_amount / oripa.supply`)
    - ランクごとの総枚数・確率%・代表カード名を算出
    - 1等+2等合計確率も算出

### パターン

- `market_analysis`
- `contrarian`
- `comparison`
- `trivia`
- `urgency`

### パターン制御

- `forcePattern` 指定時はそのパターン固定
- `urgency` は低在庫率時のみ有効（通常は強制回避）
- AI生成に失敗した場合は固定テンプレで投稿せず、`*_ai_failed` としてスキップ（ランダムdailyは他ソースへフォールバック）
- パターンはABテスト目的で残している（`market_analysis` / `contrarian` / `comparison` / `trivia` / `urgency`）。
  - 目的: 反応差を比較し、今後の最適化に使うため
  - ただし「テンプレ化」は狙っていない。テンプレ調を検出した場合はプロンプト/検証ルールを更新する

### 投稿文の冒頭ルール

- **1行目は商品名ではなく、読み手のメリット・数値的事実から始める**
  - 良い例: `最低保証1,100coin・赤字なしで引ける` / `1口999coinで〇〇狙い` / `1日1回限定で試せる`
  - NG例: `⚡黒炎の舞` のような商品名だけの冒頭
- @tcgstore_io の実績フォーマットを優先し、次の骨格を参考にする
  - `〇〇狙い、{価格}coinから。`
  - `〇〇、今ここで狙える。`
  - 2行目は「試しやすい価格帯」「入口」のような参加ハードルの低さを短く補足する
  - `相場44,800円` のような市場価格は、入力データに明示された場合のみ記載可（未提供なら記載しない）
- 1等+2等合計確率は内部判断に利用。本文では `N人に1人` と `0.x%` の確率訴求を使わない
- カード名は投稿時に整形し、`[型番]` や `（拡張パック名）` などの注釈は除去する
- 1等+2等の確率は、条件を満たす場合 `1等と2等は約N人に1人` の言い回しを優先する（事実値ベース）

### 文字数モード（lengthMode）

- **short**: URL込み60〜90文字。1〜2文でサラッと要点だけ。
- **full**: URL込み100〜140文字。しっかり情報を盛り込む。
- `dateSeed % 2` で交互に切り替わり、タイムラインに緩急をつける。

### 文面ガード（検証）

- 文字数: lengthModeに応じて short=60〜90 / full=100〜140（X換算）
- URLは末尾
- ハッシュタグ規則厳守
- 絵文字は0〜1個まで（連打禁止）
- **冒頭が商品名のみで始まっている場合はリジェクト**
- 非pOAS商品で `pOAS/OAS/演出違い` 言及禁止
- 高在庫率で煽り文言禁止
- 根拠不明な「どんな層が引いてる」禁止
- 主観感想表現（例: `正直`, `個人的に`, `〜と思う`, `〜気がする`）禁止
- 1人1回限定商品のお得訴求を必須化
- **`N人に1人` 表現を禁止**
- **`0.x%` 表現を禁止**
- 確率を根拠にした煽り表現を禁止（例: 当たりやすい/狙いやすい/期待感が上がる）
- `1等+2等合計確率` は「上位賞の目安」として扱い、全体保証（`必ず` / `全員` / `100%`）に置き換えない
- **ランク名の解釈語を禁止**（例: `Legendary以上`, `Legendaryクラス`, `上位グレード`）
- **カード名の英単語を原文から補正しない**（API原文ベースで記載）
- 保証根拠がない `必ず/確定/100%` を禁止

## ハッシュタグルール（確定）

- `market_analysis` / `trivia` / `comparison` / `contrarian`:
  - 商品名から推定した関連タグを **最大1個** のみ付与
  - 例: `ブレヒロ` を含む商品は `#ブレヒロ`
  - 商品名で推定できない場合は、上位賞/次点賞カード名から推定
  - それでも推定できない場合は **タグなし**
- `urgency`: 推定可能な関連タグ1個、推定不能ならタグなし
- `pOAS / OAS`: なし
- 非ポケカ商品へ `#ポケカ` を付与しない（例: ワンピ系は `#ワンピカード`）

## 特記事項（強制訴求）

- `1人1回限定_1000coin`:
  - 1人1回限定
  - 最低1100coin以上訴求
  - 赤字覚悟/新規向け訴求を許容
- `1人1回限定_10000coin_ピカゼク`:
  - 1人1回限定
  - 最低10000coin以上訴求
  - 赤字覚悟/新規向け訴求を許容

## 手動テスト用エンドポイント

- daily プレビュー:
  - `/?mode=tcg_daily`（現在は `tcg_daily_disabled` を返す）
- daily 実投稿:
  - `/?mode=tcg_daily&commit=1`（現在は停止中）
- メルカリdaily プレビュー:
  - `/?mode=mercari_daily`（現在は `mercari_daily_disabled` を返す）
- メルカリdaily 実投稿:
  - `/?mode=mercari_daily&commit=1`（現在は停止中）
- 市場まとめ プレビュー:
  - `/?mode=market_summary`
- 市場まとめ 実投稿:
  - `/?mode=market_summary&commit=1`
- 非pOASのみ:
  - `/?mode=tcg_daily&exclude_unit=pOAS,OAS`
- パターン固定:
  - `/?mode=tcg_daily&pattern=contrarian`
- 候補ずらし:
  - `/?mode=tcg_daily&pick_offset=3`
- サンプル一覧:
  - `/?mode=tcg_samples&count=5`
- price_spike プレビュー:
  - `/?mode=price_spike`（POST JSON / `commit` なし）
- price_spike 実投稿:
  - `/?mode=price_spike&commit=1`（POST JSON）

## 機能5: price_spike 投稿

- リクエストJSON:
  - `source: string`
  - `spikes: [{ card, variant?, card_id?, before, after, change_pct, fetched_at, period?, source_site?, source_url? }]`
- `spikes` の先頭1件のみ処理
- 成功時は KV `watchlist` を更新（新規追加 / `firstSeenAt`・`lastSeenAt` 更新 / 7日失効データを除外）
- `source_url` ページから `og:image` を抽出し、`watchlist.imageUrl` に保存
- 入力バリデーション（厳格）:
  - 参照サイトは `snkrdunk` / `pokeca-chart` のみ許可
  - `source_url` は必須（対応サイトのURLのみ許可）
  - `variant` / `card_id` が不足している場合は `source_url` のページを取得してカード識別情報を補完
  - 補完は「カード名の近傍テキストで識別子が一意に取れた場合のみ」実施（曖昧なら投稿拒否）
  - カード識別子（`card_id`）または型番・収録番号等を含む `variant` が無い場合は投稿拒否
  - 曖昧名（例: 同名カード複数が存在しうる名称のみ）は投稿しない
- 重複抑止:
  - KVキー `price_spike:{source}:{card_id or canonical_card_name}`
  - 既に存在する場合は `skipped: true` で終了
  - 実投稿成功時のみTTL 21600秒（6時間）で記録
- 投稿文:
  - Anthropic 生成（失敗時はフォールバック文）
  - 人格: ポケカ好きな情報通が、相場の動きをさらっと共有するトーン
  - 構成:
    - 1行目: `カード名 + {period}で+{変化率}%`
      - `period` 未指定時は `直近` を使う（例: `直近で+8.08%`）
    - 2行目: `{前回価格}円 → {現在価格}円（+{変化率}%）`
    - 3行目: 背景の推察をやわらかく一文
    - URLは付与しない（市場情報のみ）
  - `#ポケカ` 固定
  - 60〜100文字
  - 煽り/主観/根拠なし予測を禁止

## 必要な環境変数/Secrets

- Cloudflare:
  - `CLOUDFLARE_API_TOKEN`
- X:
  - `X_API_KEY`
  - `X_API_KEY_SECRET`
  - `X_ACCESS_TOKEN`
  - `X_ACCESS_TOKEN_SECRET`
- Anthropic:
  - `ANTHROPIC_API_KEY`
  - `ANTHROPIC_MODEL`（未設定時デフォルト利用）

